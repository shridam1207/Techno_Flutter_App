import 'package:flutter/material.dart';

class LogicQ extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Logic Q'),
      ),
      body: SafeArea(
        child: Center(
          child: Text('Welcome To LogicQ'),
        ),
      ),
    );
  }
}